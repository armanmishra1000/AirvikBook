# Copy this file to .env and update the values
# Backend Environment Variables

# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
DATABASE_URL="postgresql://postgres:password@localhost:5432/airvikbook?schema=public"

# Database Connection Pooling & SSL
DB_MAX_CONNECTIONS=20
DB_MIN_CONNECTIONS=5
DB_CONNECTION_TIMEOUT=30000
DB_IDLE_TIMEOUT=300000
DB_ACQUIRE_TIMEOUT=60000
DB_TIMEOUT=30000
DB_SSL_MODE=require
DB_SSL_ENABLED=true

# JWT Configuration
JWT_ACCESS_SECRET=your-actual-64-character-random-string-generated-with-crypto
JWT_REFRESH_SECRET=your-actual-64-character-random-string-generated-with-crypto
JWT_EXPIRE=15m
JWT_REFRESH_EXPIRE=7d

# CORS Configuration
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:5000

# API Configuration
API_PREFIX=/api/v1

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=debug

# Google OAuth (for social login)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Email Configuration (Brevo SMTP)
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-brevo-smtp-username@smtp-brevo.com
SMTP_PASS=your-brevo-smtp-password-here
FROM_EMAIL=your-email@gmail.com

# SMS Configuration (Optional - Twilio)
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_PHONE_NUMBER=your-twilio-phone

# WhatsApp Business API (Optional)
WHATSAPP_ACCESS_TOKEN=your-whatsapp-access-token
WHATSAPP_PHONE_NUMBER_ID=your-phone-number-id
# Payment Gateway Configuration
RAZORPAY_KEY_ID=your-razorpay-key-id
RAZORPAY_KEY_SECRET=your-razorpay-key-secret
ICICI_MERCHANT_ID=your-icici-merchant-id
ICICI_ENCRYPTION_KEY=your-icici-encryption-ke

# Security & Antivirus Configuration
# ClamAV Configuration (for malware scanning)
CLAMAV_PATH=clamscan
# VirusTotal API (for additional malware scanning)
VIRUSTOTAL_API_KEY=your-virustotal-api-key

# Redis Configuration (for token blacklisting)
REDIS_URL=redis://localhost:6379

# Security Headers
SECURITY_HEADERS_ENABLED=true
CSP_REPORT_URI=your-csp-report-uri

# File Upload Security
MAX_FILE_SIZE=5242880
ALLOWED_FILE_TYPES=image/jpeg,image/jpg,image/png,image/webp
SCAN_FILES_FOR_MALWARE=true

# Password Security
PASSWORD_MIN_LENGTH=12
PASSWORD_MAX_LENGTH=128
PASSWORD_REQUIRE_UPPERCASE=true
PASSWORD_REQUIRE_LOWERCASE=true
PASSWORD_REQUIRE_NUMBERS=true
PASSWORD_REQUIRE_SPECIAL_CHARS=true
PASSWORD_PREVENT_COMMON=true
PASSWORD_PREVENT_SEQUENTIAL=true
PASSWORD_PREVENT_REPEATING=true
PASSWORD_PREVENT_PERSONAL_INFO=true
PASSWORD_PREVENT_KEYBOARD_PATTERNS=true

# Audit Logging
AUDIT_LOG_ENABLED=true
AUDIT_LOG_RETENTION_DAYS=90
SECURITY_ALERT_ENABLED=true

# Session Security
SESSION_TIMEOUT_MINUTES=15
REFRESH_TOKEN_TIMEOUT_DAYS=1
MAX_SESSIONS_PER_USER=5

# Rate Limiting (Enhanced)
AUTH_RATE_LIMIT_WINDOW_MS=900000
AUTH_RATE_LIMIT_MAX_ATTEMPTS=5
REGISTRATION_RATE_LIMIT_WINDOW_MS=3600000
REGISTRATION_RATE_LIMIT_MAX_ATTEMPTS=3
EMAIL_RATE_LIMIT_WINDOW_MS=3600000
EMAIL_RATE_LIMIT_MAX_ATTEMPTS=10

# Monitoring & Alerting
SECURITY_MONITORING_ENABLED=true
ALERT_EMAIL_ENABLED=true
ALERT_EMAIL_RECIPIENTS=admin@yourdomain.com,security@yourdomain.com


# Storage Configuration
STORAGE_TYPE=s3
AWS_ACCESS_KEY_ID=your-aws-access-key-id-from-iam-user
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key-from-iam-user
AWS_REGION=eu-north-1
AWS_S3_BUCKET_NAME=airvikbook-uploads-web

# HTTPS Configuration
FORCE_HTTPS=true